# Frontend environment variables
# (Vite exposes variables prefixed with VITE_ to the client)
VITE_API_BASE_URL=

# Serverless (Vercel Function) secrets – set these in Vercel Project Settings (Environment Variables)
IMGFLIP_USERNAME=
IMGFLIP_PASSWORD=
